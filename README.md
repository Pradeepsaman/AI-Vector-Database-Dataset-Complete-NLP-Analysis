Based on your notebook, here are all the technical skills you used:

Machine Learning & NLP

TF-IDF Vectorization — with unigrams + bigrams (ngram_range=(1,2))
K-Means Clustering — with elbow method to find optimal k
Latent Dirichlet Allocation (LDA) — unsupervised topic modeling
Logistic Regression — multi-class text classification
Naive Bayes (MultinomialNB) — probabilistic text classification
Cross-Validation — 5-fold CV for model evaluation

 Dimensionality Reduction

TruncatedSVD — reducing TF-IDF matrix to 50 dimensions
t-SNE — 2D visualization of high-dimensional document embeddings

Feature Engineering

Document length, word count, unique word count, avg word length
Keyword-based topic extraction from raw text
Label encoding for classification
